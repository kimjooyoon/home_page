import 'package:flutter/material.dart';

class ProjectInfoOrganism extends StatelessWidget {
  const ProjectInfoOrganism({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
