import 'package:flutter/material.dart';

class ProjectWorkSearchMolecule extends StatelessWidget {
  const ProjectWorkSearchMolecule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
