import 'package:flutter/material.dart';

class ProjectDetailMolecule extends StatelessWidget {
  const ProjectDetailMolecule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
