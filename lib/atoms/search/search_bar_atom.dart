import 'package:flutter/material.dart';

class SearchBarAtom extends StatelessWidget {
  const SearchBarAtom({super.key});

  @override
  Widget build(BuildContext context) {
    return const SearchBar();
  }
}
